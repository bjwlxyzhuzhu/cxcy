# 部署迁移手册 · 整体迁到国内服务器（数据不出境版）

> 目标：把「双创AI星际」从 Supabase 云（境外）整体迁到一台国内 VM 上——自建 Supabase + Docker 跑网站。
> 代码零改动，只换环境变量。最佳时机：平台上线前（真实学生数据尚未产生，学生个人信息自始不出境）。
> 时间线：今天买服务器+提交备案 → 备案 2~4 周 → 8 月上线 → 9 月特训营入驻 → 10-08 收割成效数据。

## 0. 采购清单（用户自办）

- [ ] 国内云账号实名认证（腾讯云 / 阿里云任一）
- [ ] VM：**4核8G** 优先（最低 2核4G 可行），地域选上海/北京，系统选 **Ubuntu 22.04/24.04**
- [ ] 域名一个（注意双盲：不含校名/地名拼音），在同一云厂商买便于备案
- [ ] **立刻提交 ICP 备案**（免费，2~4 周；App 全程办理：身份证+人脸+接工信部电话）
- [ ] 安全组放行端口：22(SSH)、80、443；其余全关（Supabase 的 5432/8000 不对公网开放）

## 1. 服务器初始化

```bash
# SSH 登录后
apt update && apt install -y docker.io docker-compose-v2 git
systemctl enable --now docker
```

## 2. 自建 Supabase（官方 Docker 发行版）

```bash
git clone --depth 1 https://github.com/supabase/supabase
cd supabase/docker
cp .env.example .env
```

必改的 `.env` 项（其余保默认）：
- `POSTGRES_PASSWORD`：强密码
- `JWT_SECRET`：40 位以上随机串
- `ANON_KEY` / `SERVICE_ROLE_KEY`：用官方文档的 "Generate API Keys" 工具由 JWT_SECRET 生成（搜索 supabase self-hosting api keys）
- `DASHBOARD_USERNAME` / `DASHBOARD_PASSWORD`：Studio 管理台账号（等价于原来的 Supabase 控制台）
- `SITE_URL` / `API_EXTERNAL_URL` / `SUPABASE_PUBLIC_URL`：填 `https://你的域名` （备案没下来前暂填 `http://服务器IP:8000`）

启动与瘦身（2核4G 机器建议禁用非必需服务）：
```bash
# 全量启动
docker compose up -d
# 资源紧张时可停掉：analytics(logflare)、vector、edge functions（本项目不用）
docker compose stop analytics vector functions
```

验证：浏览器开 `http://IP:8000` 进 Studio（输入 DASHBOARD 账号）。

> 国内拉 Docker 镜像慢/失败：给 Docker 配国内镜像加速器（daemon.json），腾讯云/阿里云控制台有各自的加速地址文档。

## 3. 建库（跑迁移 SQL）

Studio → SQL Editor，按顺序粘贴执行 `web/supabase/migrations/` 下：
`0001_init.sql` → `0002` → `0003` → `0004` → `0005` → `0006` → `0007` → `0008_evidence.sql` → `0009_interventions.sql`
（全部幂等，可重复执行；0003 含 pgvector，自建版已内置该扩展。）

## 4. 迁数据（上线前做，量很小）

当前云端只有演示数据，两条路任选：
- **推荐 · 重建**：新库跑 `node --env-file=.env.local scripts/seed.mjs` 重建演示账号；知识库用 `scripts/ingest-*.mjs` 重灌（顺带换国产 embedding，见 §7）。
- **搬运**：如想保留云端已灌好的知识库向量（省 embedding 费用）：
  ```bash
  # 本机执行：从 Supabase 云导出仅数据（连接串在 Supabase 控制台 Database 页）
  pg_dump "postgresql://postgres:密码@db.xxx.supabase.co:5432/postgres" \
    --data-only -t public.knowledge -t public.rosters -t public.app_config > data.sql
  # 导入新库（服务器上）
  docker exec -i supabase-db psql -U postgres -d postgres < data.sql
  ```
  ⚠️ `profiles`/`auth.users` 不建议搬（演示账号重建更干净）。

## 5. Portainer 单 Stack 部署网站与 Supabase

如果使用 Portainer Web Editor，将官方 `supabase/docker/docker-compose.yml` 与仓库
`deploy/portainer/cxcy-stack.yml` 的 `cxcy` 服务合并后作为一个 Stack 部署。Portainer
环境变量中只需维护官方 Supabase 的 `API_EXTERNAL_URL`、`ANON_KEY`、`SERVICE_ROLE_KEY`
以及网站的 AI 服务变量；`cxcy` 会从同一个 Stack 自动引用 Supabase 值，不再需要 GitHub
Actions 的 Supabase Variables。

## 5.1 命令行部署网站（非 Portainer）

```bash
# 服务器上
git clone <你的代码仓库> app && cd app/web    # 或 scp 上传 web/ 目录（不含 node_modules/.next）
cp .env.example .env.local
```
`.env.local` 关键项（全部指向本机自建 Supabase）：
```
NEXT_PUBLIC_SUPABASE_URL=https://你的域名/  （或 http://IP:8000，走 Kong 网关）
NEXT_PUBLIC_SUPABASE_ANON_KEY=<§2 生成的 ANON_KEY>
SUPABASE_SERVICE_ROLE_KEY=<§2 生成的 SERVICE_ROLE_KEY>
CHAT_BASE_URL=https://api.deepseek.com  CHAT_API_KEY=...   # 国产对话模型（不变）
APIMART_*：过渡期保留；EMBED 换国产后可移除
```
```bash
docker compose --env-file .env.local up -d --build   # 项目自带 compose，起在 3000
```

## 6. HTTPS（备案通过后，用 Caddy 零维护）

```bash
apt install -y caddy
cat >/etc/caddy/Caddyfile <<'EOF'
你的域名 {
    reverse_proxy /rest/* localhost:8000
    reverse_proxy /auth/* localhost:8000
    reverse_proxy /storage/* localhost:8000
    reverse_proxy /realtime/* localhost:8000
    reverse_proxy localhost:3000
}
EOF
systemctl restart caddy   # 自动申请并续签证书
```
（简化方案：应用与 Supabase 各绑一个子域名分别反代，按需调整。）

## 6.5 商业级 PPT 引擎（Presenton，开源自建）

平台「幻灯生成」页的"一键生成商业级 PPT"依赖开源引擎 [Presenton](https://github.com/presenton/presenton)（Apache-2.0，Gamma 开源替代，直出可编辑 PPTX + 自带在线编辑器）。同一台服务器加一个容器即可：

```bash
docker run -d --name presenton --restart unless-stopped -p 5001:80 \
  -e LLM="custom" \
  -e CUSTOM_LLM_URL="https://api.deepseek.com" \
  -e CUSTOM_LLM_API_KEY="<你的 DeepSeek Key>" \
  -e CUSTOM_MODEL="deepseek-chat" \
  -e IMAGE_PROVIDER="pexels" -e PEXELS_API_KEY="<可选，免费注册，配图用>" \
  -v ./presenton_data:/app_data \
  ghcr.io/presenton/presenton:latest
```

然后在网站 `.env.local` 补三行并重启应用：
```
PPTGEN_BASE_URL=http://localhost:5001
PPTGEN_PUBLIC_URL=https://ppt.你的域名   # 浏览器下载/在线编辑用；Caddy 加一段反代到 5001
PPTGEN_AUTH=
```
说明：LLM 走 DeepSeek（内容不出境）；配图源 pexels 免费（也可留空不配图）。本地开发同理（Windows 装 Docker Desktop 后同一条命令）。

## 7. 合规加固（可选但建议）

- **embedding 换国产**：阿里 DashScope `text-embedding-v4` 或智谱 `embedding-3`（改 `EMBED_MODEL` + 网关地址，代码里嵌入调用集中在 ingest 脚本与 `/api/ai/ask`，改动很小——交给 Claude Code 做）。换模型后知识库需重灌。
- **每日备份**（crontab -e 加一行）：
  ```
  30 3 * * * docker exec supabase-db pg_dump -U postgres postgres | gzip > /backup/db-$(date +\%F).sql.gz && find /backup -mtime +14 -delete
  ```
- 合规页 `/compliance` 更新表述：数据存储于境内自建数据库、加密传输、每日备份、最小化采集。

## 8. 验收清单

- [ ] 域名 HTTPS 打开首页，深空动效正常
- [ ] 演示账号登录 → 对话扣积分 → 模拟答辩打分 → 成长星图出现星轨与夜枭卡片
- [ ] 管理端五个标签页全部可用（含 🦉 干预督导）
- [ ] 关掉一台电脑换网络再访问（验证公网稳定性）
- [ ] `/backup` 目录出现当日备份文件

## 常见坑

- 数据库 5432、Kong 8000 **不要**对公网开放（安全组只留 22/80/443）。
- 服务器上系统时间要准（JWT 校验依赖时钟）。
- 云厂商"未备案域名访问 80/443 会被拦截"——备案下来前用 IP:3000 兜底演示。
- 双盲：域名 whois、页面页脚、Studio 组织名均不出现校名/地名。

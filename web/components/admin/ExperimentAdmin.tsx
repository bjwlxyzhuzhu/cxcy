/* eslint-disable @typescript-eslint/no-explicit-any */
"use client";
import { useState } from "react";
export default function ExperimentAdmin(){const [title,setTitle]=useState("AI学习与就业陪伴平台课堂实验"),[run,setRun]=useState<any>(null),[msg,setMsg]=useState(""); async function create(){const r=await fetch("/api/experiment",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"teacher_create",title})});const d=await r.json();if(!r.ok){setMsg(d.error);return}setRun(d);setMsg("实验已创建，请把实验码发给学生")}; async function start(){const r=await fetch("/api/experiment",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"teacher_start",runId:run.id})});const d=await r.json();setRun(d);setMsg(r.ok?"计时已开始，50分钟后自动结束":d.error||"启动失败")}; return <div style={{border:"1px solid var(--line)",borderRadius:16,padding:18,background:"rgba(255,255,255,.04)"}}><h3 style={{marginTop:0}}>🛰️ 多智能体双层对抗实验</h3><p style={{color:"var(--mut)",fontSize:13,lineHeight:1.7}}>创建后系统按加入顺序交替分配“综合专家组 / 五专家轮询组”，服务端统一计时并记录 V0→V1→V2、T0/T1 和完整审辩链。</p>{!run?<><input value={title} onChange={e=>setTitle(e.target.value)} style={{padding:10,borderRadius:9,border:"1px solid var(--line)",background:"transparent",color:"var(--ink)",width:"min(100%,420px)"}}/><button onClick={create} style={{marginLeft:10,padding:"10px 15px",border:0,borderRadius:9,fontWeight:800,background:"linear-gradient(120deg,#22d3ee,#7c5cff)"}}>创建实验</button></>:<><div style={{fontSize:28,fontWeight:900,letterSpacing:4,color:"var(--cyan)"}}>{run.join_code}</div><div style={{margin:"8px 0",color:"var(--mut)"}}>状态：{run.status}</div>{run.status!=="active"&&<button onClick={start} style={{padding:"10px 15px",border:0,borderRadius:9,fontWeight:800,background:"#7ef0c0"}}>开始 50 分钟计时</button>}</>}{msg&&<p style={{color:"var(--mut)"}}>{msg}</p>}</div>}

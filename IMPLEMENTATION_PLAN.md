# 测试流程与数据留存改进

1. 已完成：核对原始材料及代码，确认阶段强制切换、AI问句未保存、缺少恢复入口和稳定导出编号。验证：读取前后测列定义及实验接口。
2. 已完成：实现可恢复的实验流程、各模块完整记录、统一导出及后台查看。验证：类型检查、4项协议/迁移/导出测试、真实HTTP数据库集成测试通过。
3. 已完成本地验证：浏览器检查学生历史、继续练习、管理员记录及在线状态；生产模式构建与集成测试通过。Windows本机无符号链接权限，验证时设置 NEXT_DISABLE_STANDALONE=1；Linux/Docker仍默认使用standalone。pnpm 9冻结锁文件验证通过。
4. 发布：提交源码至 main，推送后核对远端提交。GitHub既有Docker工作流构建镜像；不把提交源码视为线上服务已更新。

原始学生材料仅在本地用于需求分析，不提交到代码仓库。不按问卷序号或 IP 配对历史数据，不将未完成作为零分。

## 实验操作与跨模块改进

1. 已完成：核查两次点击、单一题材及普通模块被拦回的问题。验证：阅读学生页面、状态机、路由拦截。
2. 已完成：增加案例快照、逐步指导、原子提交跳转及三个模块共用实验工作台。验证：新旧课堂迁移、HTTP流程、内容传递及恢复测试通过。
3. 已完成验证：浏览器走完实验三的教师创建开放及学生全部步骤、草稿刷新、跨模块带入、跳过和多场实验恢复。修正导出框对比度与保存提示。5项自动测试、生产构建及生产HTTP集成测试通过（本地AI替身，Windows关闭standalone打包）。本次源代码提交推送至main，随后核对远端。

## 督导与分析导出改进

1. 已完成：查明历史督导依赖打开成长星图、七天去重及未接入课堂记录；导出原始字段不便按分组/阶段分析。验证：读取触发链和报告结构。
2. 已完成：每人历史综合卡、自动事件提示、去重与备注保留、后台筛选；分表导出及北京时间与阶段经过时长；管理员专属全体教学导出入口和接口权限。
3. 已完成验证：7项协议/迁移/报告测试、开发及生产HTTP集成测试、生产构建通过；页面核对管理员可见且普通教师不可见全体导出。验证学生/教师/未登录导出被拒绝，历史账号补卡、无记录标注、撤回保留、七种格式全体导出。AI使用本地替身，未操作生产学生数据库。源码提交推送main后核对远端；保留缺失数据，不按IP猜测归属、不补造历史时间。
